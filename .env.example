# Django backend API base URL (include /api)
API_BASE_URL=
# Optional: save the latest captured screen region to debug-captures/latest.png
DEBUG_SAVE_CAPTURE=false
# Optional: print OCR loop debug logs in Electron main process
DEBUG_OCR_LOG=false